export const state = () => ({
  isShowModal: false
})
export const mutations = {
  SHOW_SEARCHMODAL(state, payload) {
    state.isShowModal = payload
  }
}

export const getters = {
}
export const actions = {
}
